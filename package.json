{
  "name": "nepali-number-converter",
  "version": "1.0.4",
  "description": "A package to convert numbers to their Nepali word representation.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "npx prettier --write src/**/*",
    "format:check": "npx prettier --check src/**/*",
    "lint": "eslint --ext .ts src",
    "lint:fix": "eslint --quiet --ext .ts src --fix"
  },
  "keywords": [
    "nepali",
    "number",
    "converter"
  ],
  "author": "Your Name",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^4.9.5",
    "eslint": "^8.35.0",
    "eslint-config-prettier": "^8.6.0",
    "eslint-config-standard-with-typescript": "^34.0.0"
  }
}
